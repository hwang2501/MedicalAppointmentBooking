/** Ngày local theo ISO yyyy-MM-dd (cho input type="date") */
export function todayISODate() {
  const d = new Date();
  const p = (n) => String(n).padStart(2, '0');
  return `${d.getFullYear()}-${p(d.getMonth() + 1)}-${p(d.getDate())}`;
}
